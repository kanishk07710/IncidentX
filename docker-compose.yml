services:
  db:
    image: postgres:16-alpine
    container_name: incidentx-db
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: incidentx
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
